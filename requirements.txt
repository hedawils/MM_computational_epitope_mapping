biopython>=1.87
numpy
scipy
matplotlib

# External binaries (not pip-installable):
#   mafft   - brew install mafft   (CDR loop alignment)
#   blast+  - brew install blast   (blastp/makeblastdb, VH germline assignment)
